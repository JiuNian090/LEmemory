/* ========== CSS变量定义 ========== */
page {
  --primary-color: #34d399;
  --primary-dark: #10b981;
  --primary-light: #d1fae5;
  --danger-color: #f87171;
  --warning-color: #fbbf24;
  --info-color: #60a5fa;
  --bg-color: #f5f5f5;
  --card-bg: #ffffff;
  --text-primary: #1a1a1a;
  --text-secondary: #666666;
  --text-tertiary: #999999;
  --border-color: #e8e8e8;
  --shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.05);
  --shadow-hover: 0 8rpx 24rpx rgba(0, 0, 0, 0.08);
  --radius-sm: 8rpx;
  --radius-md: 16rpx;
  --radius-lg: 24rpx;
  --spacing-sm: 20rpx;
  --spacing-md: 30rpx;
  --spacing-lg: 40rpx;
  --header-gradient: linear-gradient(135deg, #34d399 0%, #10b981 100%);
  --header-text: #ffffff;
  --header-text-secondary: rgba(255, 255, 255, 0.9);
  --danger-gradient: linear-gradient(135deg, #f87171, #ef4444);
  --mask-bg: rgba(0, 0, 0, 0.5);
  --shadow-heavy: 0 16rpx 48rpx rgba(0, 0, 0, 0.12);
  --shadow-card: 0 4rpx 20rpx rgba(0, 0, 0, 0.08);
  --font-size-xxl: 36rpx;
  --font-size-xl: 32rpx;
  --font-size-lg: 28rpx;
  --font-size-md: 26rpx;
  --font-size-sm: 24rpx;
  --font-size-xs: 22rpx;
  --spacing-xs: 16rpx;
  --spacing-xxs: 12rpx;
  --radius-xl: 40rpx;
  --btn-height: 88rpx;
  --btn-height-sm: 80rpx;
  --gradient-data: linear-gradient(135deg, #e6f7ff 0%, #f0f9ff 100%);
  --gradient-cloud: linear-gradient(135deg, #f0f5ff 0%, #f5f8ff 100%);
  --gradient-about: linear-gradient(135deg, #fff7e6 0%, #fff9f0 100%);
  --gradient-changelog: linear-gradient(135deg, #f6ffed 0%, #f0fff0 100%);
  --gradient-indigo: linear-gradient(135deg, #6366f1 0%, #4f46e5 100%);
  --mastery-not-started: #d1d5db;
  --mastery-basic: #fbbf24;
  --mastery-good: #34d399;
  --mastery-difficult: #f87171;
  
  background-color: var(--bg-color);
  font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', 'PingFang SC', 'Hiragino Sans GB', 'Microsoft YaHei', sans-serif;
}

/* ========== 深色模式变量覆盖 ========== */
@media (prefers-color-scheme: dark) {
  page {
    --primary-color: #34d399;
    --primary-dark: #10b981;
    --primary-light: #064e3b;
    --danger-color: #f87171;
    --warning-color: #fbbf24;
    --info-color: #60a5fa;
    --bg-color: #1a1a1a;
    --card-bg: #262626;
    --text-primary: #e5e5e5;
    --text-secondary: #a3a3a3;
    --text-tertiary: #737373;
    --border-color: #333333;
    --shadow: 0 2rpx 8rpx rgba(0, 0, 0, 0.3);
    --shadow-hover: 0 8rpx 24rpx rgba(0, 0, 0, 0.35);
    --header-gradient: linear-gradient(135deg, #059669 0%, #047857 100%);
    --header-text: #ffffff;
    --header-text-secondary: rgba(255, 255, 255, 0.85);
    --danger-gradient: linear-gradient(135deg, #ef4444, #dc2626);
    --mask-bg: rgba(0, 0, 0, 0.6);
    --shadow-heavy: 0 16rpx 48rpx rgba(0, 0, 0, 0.35);
    --shadow-card: 0 4rpx 20rpx rgba(0, 0, 0, 0.3);
    --font-size-xxl: 36rpx;
    --font-size-xl: 32rpx;
    --font-size-lg: 28rpx;
    --font-size-md: 26rpx;
    --font-size-sm: 24rpx;
    --font-size-xs: 22rpx;
    --spacing-xs: 16rpx;
    --spacing-xxs: 12rpx;
    --radius-xl: 40rpx;
    --btn-height: 88rpx;
    --btn-height-sm: 80rpx;
    --gradient-data: linear-gradient(135deg, #0c2d3f 0%, #0a2e3a 100%);
    --gradient-cloud: linear-gradient(135deg, #1a1040 0%, #1f1245 100%);
    --gradient-about: linear-gradient(135deg, #3a2a0a 0%, #3f2e0a 100%);
    --gradient-changelog: linear-gradient(135deg, #0a2e1a 0%, #0a2e1a 100%);
    --gradient-indigo: linear-gradient(135deg, #1a1040 0%, #1f1245 100%);
    --mastery-not-started: #6b7280;
    --mastery-basic: #fbbf24;
    --mastery-good: #34d399;
    --mastery-difficult: #f87171;
  }
}

/* 深色模式下的图标背景覆盖 */
@media (prefers-color-scheme: dark) {
  .data-icon,
  .export-btn,
  .profile-icon {
    background: linear-gradient(135deg, #0c2d3f 0%, #0a2e3a 100%) !important;
  }
  
  .cloud-icon {
    background: linear-gradient(135deg, #1a1040 0%, #1f1245 100%) !important;
  }
  
  .about-icon {
    background: linear-gradient(135deg, #3a2a0a 0%, #3f2e0a 100%) !important;
  }
  
  .changelog-icon,
  .share-btn,
  .import-btn {
    background: linear-gradient(135deg, #0a2e1a 0%, #0a2e1a 100%) !important;
  }
  
  .clear-btn {
    background: linear-gradient(135deg, #3a1010 0%, #3f1212 100%) !important;
  }
  
  .security-icon {
    background: linear-gradient(135deg, #3a350a 0%, #3f3a0a 100%) !important;
  }
}

/* ========== 容器 ========== */
.container {
  min-height: 100vh;
  padding-bottom: var(--spacing-lg);
  opacity: 1;
  transition: opacity 0.2s ease-in-out;
  box-sizing: border-box;
}

/* ========== 页面标题区 ========== */
.page-header {
  position: relative;
  margin: var(--spacing-md);
  padding: 64rpx var(--spacing-md);
  background: var(--header-gradient);
  border-bottom: 2rpx solid var(--border-color);
  box-shadow: var(--shadow);
  border-radius: var(--radius-lg);
  opacity: 1;
  transform: translateY(0);
  transition: all 0.3s ease-in-out;
}

.header-content {
  position: relative;
  z-index: 1;
  text-align: center;
}

.header-title {
  font-size: 36rpx;
  font-weight: 600;
  color: var(--header-text);
  margin-bottom: var(--spacing-sm);
  text-shadow: 0 1rpx 2rpx rgba(0, 0, 0, 0.1);
  display: block;
}

.header-subtitle {
  font-size: var(--font-size-sm);
  color: var(--header-text-secondary);
  font-weight: 400;
}

/* ========== 区块卡片 ========== */
.section {
  background: var(--card-bg);
  border-radius: var(--radius-lg);
  margin: 0 var(--spacing-md) var(--spacing-md);
  box-shadow: var(--shadow-card);
  overflow: hidden;
  transform: translateY(0);
  opacity: 1;
  transition: all 0.3s ease;
}

.section-header {
  display: flex;
  align-items: center;
  padding: var(--spacing-md);
  position: relative;
  justify-content: space-between;
}

.section-icon {
  width: 48rpx;
  height: 48rpx;
  font-size: var(--font-size-xxl);
  border-radius: var(--radius-md);
  display: flex;
  align-items: center;
  justify-content: center;
  margin-right: var(--spacing-xs);
}

.data-icon {
  background: var(--gradient-data);
}

.cloud-icon {
  background: var(--gradient-cloud);
}

.about-icon {
  background: var(--gradient-about);
}

.changelog-icon {
  background: var(--gradient-changelog);
}

.icon-text {
  font-size: 32rpx;
}

.section-title {
  flex: 1;
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
}

.section-body {
  padding: 0 var(--spacing-md) var(--spacing-md);
}

/* ========== 按钮网格 ========== */
.btn-grid {
  display: grid;
  grid-template-columns: repeat(2, 1fr);
  gap: var(--spacing-sm);
}

.grid-btn {
  display: flex;
  flex-direction: column;
  align-items: center;
  justify-content: center;
  padding: 32rpx var(--spacing-sm);
  background: var(--bg-color);
  border-radius: var(--radius-md);
  transition: all 0.2s ease;
}

.grid-btn:active {
  transform: scale(0.96);
  opacity: 0.9;
}

.btn-icon {
  font-size: 40rpx;
  margin-bottom: var(--spacing-xxs);
}

.btn-text {
  font-size: var(--font-size-md);
  color: var(--text-secondary);
  font-weight: 500;
}

/* 按钮颜色 */
.export-btn {
  background: var(--gradient-data);
}

.share-btn {
  background: var(--gradient-changelog);
}

.import-btn {
  background: var(--gradient-changelog);
}

.clear-btn {
  background: linear-gradient(135deg, #fff2f0 0%, #fff5f5 100%);
}

.clear-btn .btn-text {
  color: var(--danger-color);
}

/* ========== 弹窗样式 ========== */
.modal-mask {
  position: fixed;
  top: 0;
  left: 0;
  right: 0;
  bottom: 0;
  background: var(--mask-bg);
  z-index: 998;
  opacity: 0;
  transition: opacity 0.3s ease;
}

.modal-mask.show {
  opacity: 1;
}

.modal-container {
  position: fixed;
  top: 50%;
  left: 50%;
  transform: translate(-50%, -50%) scale(0.9);
  width: 85%;
  max-width: 600rpx;
  background: var(--card-bg);
  border-radius: var(--radius-lg);
  z-index: 999;
  opacity: 0;
  transition: all 0.3s ease;
  box-shadow: var(--shadow-heavy);
}

.modal-container.show {
  opacity: 1;
  transform: translate(-50%, -50%) scale(1);
}

.modal-header {
  display: flex;
  align-items: center;
  justify-content: center;
  padding: 28rpx;
  border-bottom: 2rpx solid var(--border-color);
  position: relative;
}

.modal-title {
  font-size: 32rpx;
  font-weight: 600;
  color: var(--text-primary);
  flex: 1;
  text-align: center;
  padding-left: 48rpx;
  padding-right: 48rpx;
}

.modal-close {
  position: absolute;
  right: 28rpx;
  top: 50%;
  transform: translateY(-50%);
  width: 48rpx;
  height: 48rpx;
  display: flex;
  align-items: center;
  justify-content: center;
  border-radius: 50%;
  background: var(--bg-color);
}

.modal-close:active {
  background: var(--border-color);
}

.close-icon {
  font-size: 36rpx;
  color: var(--text-tertiary);
  line-height: 1;
}

.modal-body {
  padding: 28rpx;
  display: flex;
  flex-direction: column;
  min-height: 0;
}

.modal-footer {
  display: flex;
  gap: 20rpx;
  padding: 0 28rpx 28rpx;
  flex-direction: row;
  justify-content: space-between;
}

.modal-btn {
  flex: 1;
  height: var(--btn-height);
  line-height: var(--btn-height);
  text-align: center;
  border-radius: var(--radius-md);
  font-size: var(--font-size-lg);
  font-weight: 500;
  border: none;
  padding: 0;
  margin: 0;
}

.btn-cancel {
  background: var(--bg-color);
  color: var(--text-secondary);
}

.btn-cancel:active {
  background: var(--border-color);
}

.btn-confirm {
  background: var(--header-gradient);
  color: var(--header-text);
}

.btn-confirm:active {
  opacity: 0.9;
}

/* ========== 表单样式 ========== */
.form-group {
  margin-bottom: var(--spacing-sm);
}

.form-group:last-child {
  margin-bottom: 0;
}

.form-label {
  display: block;
  font-size: var(--font-size-md);
  color: var(--text-secondary);
  margin-bottom: var(--spacing-xxs);
}

.form-input {
  height: var(--btn-height);
  background: var(--bg-color);
  border-radius: var(--radius-md);
  padding: 0 var(--spacing-sm);
  font-size: var(--font-size-lg);
  color: var(--text-primary);
}

/* ========== 响应式适配 ========== */
@media (max-width: 375px) {
  .btn-grid {
    grid-template-columns: repeat(2, 1fr);
    gap: 16rpx;
  }

  .grid-btn {
    padding: 24rpx 16rpx;
  }

  .btn-icon {
    font-size: 36rpx;
  }

  .btn-text {
    font-size: 24rpx;
  }
}

/* 平板设备适配 */
@media (min-width: 768px) {
  .container {
    max-width: 750rpx;
    margin: 0 auto;
  }

  .btn-grid {
    grid-template-columns: repeat(4, 1fr);
  }

  /* 页面标题区平板适配 */
  .page-header {
    margin: 40rpx 40rpx 40rpx;
    padding: 80rpx 40rpx 80rpx;
  }

  .header-title {
    font-size: 40rpx;
  }

  .header-subtitle {
    font-size: 28rpx;
  }

  /* 区块卡片平板适配 */
  .section {
    margin: 0 40rpx 40rpx;
  }

  .section-header {
    padding: 40rpx;
  }

  .section-body {
    padding: 0 40rpx 40rpx;
  }

  /* 按钮网格平板适配 */
  .grid-btn {
    padding: 40rpx 24rpx;
  }

  .btn-icon {
    font-size: 48rpx;
  }

  .btn-text {
    font-size: 30rpx;
  }
}

/* 横屏适配 */
@media (orientation: landscape) {
  .container {
    padding-bottom: 20rpx;
  }
  
  .page-header {
    margin: 20rpx 20rpx 20rpx;
    padding: 40rpx 30rpx 40rpx;
  }
  
  .section {
    margin: 0 20rpx 20rpx;
  }
  
  /* 横屏时的布局调整 */
  .sections-container {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 20rpx;
  }
}

/* ========== 动效定义 ========== */

/* 列表卡牌渐入上移动效 */
@keyframes fadeInUp {
  from {
    opacity: 0;
    transform: translateY(20rpx);
  }
  to {
    opacity: 1;
    transform: translateY(0);
  }
}

/* 空状态图标浮动 */
@keyframes float {
  0%, 100% {
    transform: translateY(0);
  }
  50% {
    transform: translateY(-10rpx);
  }
}

/* 弹窗缩放入场 */
@keyframes scaleIn {
  from {
    opacity: 0;
    transform: scale(0.9);
  }
  to {
    opacity: 1;
    transform: scale(1);
  }
}

/* 淡入动效 */
@keyframes fadeIn {
  from { opacity: 0; }
  to { opacity: 1; }
}

/* ========== 减少动效模式 ========== */
@media (prefers-reduced-motion: reduce) {
  page,
  .card-list,
  .backup-list,
  .modal-content,
  .modal-container,
  .empty-icon,
  .swipe-content {
    animation: none !important;
    transition: none !important;
  }
}
