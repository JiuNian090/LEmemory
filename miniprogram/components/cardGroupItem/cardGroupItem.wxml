<view class="card-group-item">
  <view class="item-emoji">{{emoji}}</view>
  <view class="item-title">{{title}}</view>
  <view class="item-desc" wx:if="{{description}}">{{description}}</view>
  <view class="item-time">{{updateTime}}</view>
</view>
